import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Core/Overview" />

# Core UI Library

This project uses `@ui/core` as the primary UI contract surface.

## Discovery checklist

- Read stories before introducing new UI abstractions.
- Prefer composition of existing primitives.
- Add or update stories in the same change when primitives change.
